# Use default pnpm store inside the repo (node_modules/.pnpm).
# Do NOT point store-dir/virtual-store-dir at /tmp — Render (and other hosts)
# package only the app directory, so symlinks into /tmp break at runtime.
